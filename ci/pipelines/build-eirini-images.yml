## ======================================================================
## GENERATED FILE. DO NOT EDIT
## Generated by running ./configure build-eirini-images from ~/workspace/cf-for-k8s/ci
## ======================================================================

resources:
- name: eirini
  type: git
  source:
    uri: git@github.com:cloudfoundry-incubator/eirini.git
    private_key: ((cf_for_k8s_readwrite_deploy_key.private_key))
- name: eirini-release
  type: github-release
  icon: github
  source:
    owner: cloudfoundry-incubator
    repository: eirini-release
    access_token: ((cf_deployment_release_bot_access_token))
jobs:
- name: build-opi
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s 2e3a850134cbcef0229402399ea70fa73bf634f0 ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      run:
        path: /bin/bash
        args:
        - -c
        - |
          set -e
          pushd eirini
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-opi
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/opi/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-opi
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git eirini \
            --output-tar deplab-image/image.tar
  - task: publish-opi
    privileged: true
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: cfpersi/smb-k8s-kind-tests
          tag: latest
      inputs:
      - name: deplab-image
      - name: eirini-release
      params:
        DOCKER_USERNAME: ((dockerhub.username))
        DOCKER_PASSWORD: ((dockerhub.password))
        REPOSITORY: cloudfoundry/eirini-opi-cf-for-k8s
      run:
        path: /bin/sh
        args:
        - -c
        - |
          /etc/init.d/docker start
          docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
          set -x
          docker load -i deplab-image/image.tar
          image_id=$(docker images --format '{{.ID}}' | head -n 1)
          tag=$(cat eirini-release/version)
          docker tag "${image_id}" "${REPOSITORY}":"${tag}"
          docker push "${REPOSITORY}":"${tag}"
          /etc/init.d/docker stop
- name: build-event-reporter
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s 2e3a850134cbcef0229402399ea70fa73bf634f0 ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      run:
        path: /bin/bash
        args:
        - -c
        - |
          set -e
          pushd eirini
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-event-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/event-reporter/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-event-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git eirini \
            --output-tar deplab-image/image.tar
  - task: publish-event-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: cfpersi/smb-k8s-kind-tests
          tag: latest
      inputs:
      - name: deplab-image
      - name: eirini-release
      params:
        DOCKER_USERNAME: ((dockerhub.username))
        DOCKER_PASSWORD: ((dockerhub.password))
        REPOSITORY: cloudfoundry/eirini-event-reporter-cf-for-k8s
      run:
        path: /bin/sh
        args:
        - -c
        - |
          /etc/init.d/docker start
          docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
          set -x
          docker load -i deplab-image/image.tar
          image_id=$(docker images --format '{{.ID}}' | head -n 1)
          tag=$(cat eirini-release/version)
          docker tag "${image_id}" "${REPOSITORY}":"${tag}"
          docker push "${REPOSITORY}":"${tag}"
          /etc/init.d/docker stop
- name: build-eirini-controller
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s 2e3a850134cbcef0229402399ea70fa73bf634f0 ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      run:
        path: /bin/bash
        args:
        - -c
        - |
          set -e
          pushd eirini
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-eirini-controller
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/eirini-controller/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-eirini-controller
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git eirini \
            --output-tar deplab-image/image.tar
  - task: publish-eirini-controller
    privileged: true
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: cfpersi/smb-k8s-kind-tests
          tag: latest
      inputs:
      - name: deplab-image
      - name: eirini-release
      params:
        DOCKER_USERNAME: ((dockerhub.username))
        DOCKER_PASSWORD: ((dockerhub.password))
        REPOSITORY: cloudfoundry/eirini-eirini-controller-cf-for-k8s
      run:
        path: /bin/sh
        args:
        - -c
        - |
          /etc/init.d/docker start
          docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
          set -x
          docker load -i deplab-image/image.tar
          image_id=$(docker images --format '{{.ID}}' | head -n 1)
          tag=$(cat eirini-release/version)
          docker tag "${image_id}" "${REPOSITORY}":"${tag}"
          docker push "${REPOSITORY}":"${tag}"
          /etc/init.d/docker stop
- name: build-task-reporter
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s 2e3a850134cbcef0229402399ea70fa73bf634f0 ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      run:
        path: /bin/bash
        args:
        - -c
        - |
          set -e
          pushd eirini
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-task-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/task-reporter/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-task-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git eirini \
            --output-tar deplab-image/image.tar
  - task: publish-task-reporter
    privileged: true
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: cfpersi/smb-k8s-kind-tests
          tag: latest
      inputs:
      - name: deplab-image
      - name: eirini-release
      params:
        DOCKER_USERNAME: ((dockerhub.username))
        DOCKER_PASSWORD: ((dockerhub.password))
        REPOSITORY: cloudfoundry/eirini-task-reporter-cf-for-k8s
      run:
        path: /bin/sh
        args:
        - -c
        - |
          /etc/init.d/docker start
          docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
          set -x
          docker load -i deplab-image/image.tar
          image_id=$(docker images --format '{{.ID}}' | head -n 1)
          tag=$(cat eirini-release/version)
          docker tag "${image_id}" "${REPOSITORY}":"${tag}"
          docker push "${REPOSITORY}":"${tag}"
          /etc/init.d/docker stop
- name: build-instance-index-env-injector
  plan:
  - task: pipeline-revision-info
    config:
      platform: linux
      image_resource:
        source:
          repository: busybox
        type: docker-image
      run:
        path: /bin/echo
        args:
        - -e
        - git@github.com:cloudfoundry/cf-for-k8s 2e3a850134cbcef0229402399ea70fa73bf634f0 ci/templates/build-eirini-images.yml \e[31mdirty\e[0m
  - get: eirini
  - get: eirini-release
    trigger: true
  - task: rev-parse
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: bitnami/git
      inputs:
      - name: eirini
      outputs:
      - name: revision
      run:
        path: /bin/bash
        args:
        - -c
        - |
          set -e
          pushd eirini
            git rev-parse HEAD > ../revision/revision
            git describe --tags > ../revision/tag
          popd > /dev/null
  - task: build-instance-index-env-injector
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: vito/oci-build-task
      inputs:
      - name: eirini
      - name: revision
      outputs:
      - name: image
      caches:
      - path: cache
      run:
        path: /bin/sh
        args:
        - -c
        - |
          export BUILD_ARG_GIT_SHA="$(cat revision/revision)"
          ls -R
          build
      params:
        DOCKERFILE: ./eirini/docker/instance-index-env-injector/Dockerfile
        CONTEXT: ./eirini/
  - task: deplab-instance-index-env-injector
    privileged: true
    config:
      platform: linux
      image_resource:
        type: registry-image
        source:
          repository: relintdockerhubpushbot/cf-for-k8s-deplab
      inputs:
      - name: eirini
      - name: image
      outputs:
      - name: deplab-image
      run:
        path: /bin/sh
        args:
        - -c
        - |
          deplab --image-tar image/image.tar \
            --git eirini \
            --output-tar deplab-image/image.tar
  - task: publish-instance-index-env-injector
    privileged: true
    config:
      platform: linux
      image_resource:
        type: docker-image
        source:
          repository: cfpersi/smb-k8s-kind-tests
          tag: latest
      inputs:
      - name: deplab-image
      - name: eirini-release
      params:
        DOCKER_USERNAME: ((dockerhub.username))
        DOCKER_PASSWORD: ((dockerhub.password))
        REPOSITORY: cloudfoundry/eirini-instance-index-env-injector-cf-for-k8s
      run:
        path: /bin/sh
        args:
        - -c
        - |
          /etc/init.d/docker start
          docker login -u "${DOCKER_USERNAME}" -p "${DOCKER_PASSWORD}"
          set -x
          docker load -i deplab-image/image.tar
          image_id=$(docker images --format '{{.ID}}' | head -n 1)
          tag=$(cat eirini-release/version)
          docker tag "${image_id}" "${REPOSITORY}":"${tag}"
          docker push "${REPOSITORY}":"${tag}"
          /etc/init.d/docker stop
